import { prisma } from '../src/lib/prisma';


async function seedHoneypots() {
  ...');

  // 1. Canary Lead (Lead Isca)
  const canaryLead = await prisma.lead.upsert({
    where: { email: 'canary-42@zehla-security.io' },
    update: {},
    create: {
      name: 'CANARY_RESERVE_DO_NOT_TOUCH',
      email: 'canary-42@zehla-security.io',
      phone: '5511999999999',
      source: 'HONEPOT_TRAP',
      status: 'BLACKLISTED',
      score: 0,
      metadata: 'SYSTEM_INTERNAL_PII_TRAP',
      isCanary: true
    }
  });

  // 2. Canary Property (Propriedade Isca)
  // Primeiro buscamos um admin real ou criamos um mock
  const admin = await prisma.user.findFirst({ where: { role: 'ADMIN' } });
  
  if (!admin) {
    
  } else {
    const canaryProperty = await prisma.property.upsert({
      where: { slug: 'canary-pousada' },
      update: {},
      create: {
        name: 'Pousada Canary (Segurança)',
        slug: 'canary-pousada',
        address: 'Rua das Armadilhas, 0',
        isCanary: true,
        status: 'SUSPENDED',
        userId: admin.id
      }
    });
    
  }

  
}

seedHoneypots().catch(console.error);

// Vou conferir o schema antes de rodar para ver quais tabelas tem isCanary
