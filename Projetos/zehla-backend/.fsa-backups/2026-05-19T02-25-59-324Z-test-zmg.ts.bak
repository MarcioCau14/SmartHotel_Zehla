/**
 * ZMG flow test script
 * Run with: npx tsx src/lib/zmg/test-zmg.ts
 */

import { ZMG } from './core';

async function test() {
  
  
  const result = await ZMG.receive({
    agentId: 'ZCC-FALLBACK-TEST',
    propertyId: 'cmou86zq80002u19mz296ly9x',
    recipientPhone: '+5511999999999',
    recipientName: 'Marcio Cau',
    messageType: 'transactional',
    objective: 'confirm_booking',
    context: {
      customVariables: {
        content: 'Olá {{NOME}}! Sua reserva no ZEHLA foi confirmada. 🚀 (Se via SMS, o emoji sumirá)',
        nome: 'Marcio Cau'
      }
    }
  });

  );
}

test().catch(console.error);
