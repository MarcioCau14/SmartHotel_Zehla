/**
 * ZEHLA SMARTHOTEL — Posterior composite key
 * Módulo: src/domain/decision/models/PosteriorKey.ts
 */

export interface PosteriorKey {
  readonly bucketId: string;
  readonly providerName: string;
}
